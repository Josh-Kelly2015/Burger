<h2> Not eated burgers</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block eated=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Eated Burgers</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block eated=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Cook a hamburger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burg">whatchya havin</label>
    <input type="text" id="burg" name="name">
  </div>

  <div class="form-group">
    <label for="devured">how full r u</label>
    <br>
    <input type="radio" name="devoured" value="1"> i can eat<br>
    <input type="radio" name="devoured" value="0"> so full
  </div>

  <button type="submit">cook it medium-rare</button>
</form>

